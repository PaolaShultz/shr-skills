interface:
  display_name: "Fructal Cap Design"
  short_description: "Make constrained workflows feel natural"
  default_prompt: "Use $fructal on this constrained workflow: [describe the workflow, desired outcome, and modification boundary]. Treat an explicitly requested mode as the maximum outcome; otherwise select the mode from that scope."
