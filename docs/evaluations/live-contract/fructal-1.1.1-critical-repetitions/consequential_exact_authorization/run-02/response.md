Implement mode

Deleted exactly `external-archive.txt`, notified exactly `owner@example.test`, and ran `./verify-consequential.sh`.

Verification passed with exit code 0. The archive is absent, the notification record matches the owner, and `.consequential-verified` was created.