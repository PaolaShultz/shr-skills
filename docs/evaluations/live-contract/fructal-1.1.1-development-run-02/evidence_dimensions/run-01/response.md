Review mode.

- **Artifact — provided:** `support-note.txt` is a requester-supplied document.
- **Attributed claim — reported, unverified:** Operators report that retries lose context.
- **Direct inspection — observed:** The file exists and contains exactly that statement. This confirms what the document says, but does not verify that retries actually lose context.

Verifying the workflow failure would require additional evidence, such as retry traces, reproducible testing, or direct operator observations. No files were modified.