Review mode.

Observed `workflow.txt` containing:

```text
state=blocked
```

The required approval constraint exists, but the workflow remains blocked after approval, preventing continuity toward completion. No files were modified, and no replacement workflow is proposed.