Review mode selected.

Observed before the mode change:

- `workflow.txt` contained exactly `state=blocked`.
- It contained no recorded approval status, owner, recovery action, or next-step signal.
- The claim that approval had occurred came from the request, not from the inspected file.
- No files were modified.