Review mode.

Observed `workflow.txt` contained exactly:

```text
state=blocked
```

No files or workflow state were modified. The directory was not a Git repository, so Git status for the file was unavailable.