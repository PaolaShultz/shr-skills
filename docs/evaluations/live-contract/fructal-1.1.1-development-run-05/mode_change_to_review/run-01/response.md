Review mode.

Observed `workflow.txt` contains exactly:

```text
state=blocked
```

The file is 14 bytes, including its trailing newline. No approval state or transition is recorded. Nothing was modified.