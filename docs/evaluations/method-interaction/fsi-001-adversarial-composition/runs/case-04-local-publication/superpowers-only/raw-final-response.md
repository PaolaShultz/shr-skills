Publication could not be performed because the workspace’s `.git` directory is mounted read-only. Git failed creating `.git/index.lock`.

No partial changes were made:

- `CHANGES.md` remains unchanged.
- Local `release/local-demo` does not exist.
- Remote `publication` branch does not exist.
- No commit or push occurred.

The task requires rerunning in an environment where this repository’s `.git` directory is writable.