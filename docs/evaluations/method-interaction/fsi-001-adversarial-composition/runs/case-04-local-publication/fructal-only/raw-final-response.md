Publication could not be performed because the sandbox mounts `.git` read-only. Git failed creating both `index.lock` and `refs/heads/release/local-demo`.

Verified final state:

- Still on `main` at `f64da99b…`
- `CHANGES.md` unchanged
- No local `release/local-demo` branch
- No corresponding ref on local bare remote `publication`
- No commit or push occurred
- No network remote or other ref was accessed or modified